apiVersion: v1
kind: Service

metadata:
  name: kuberag-service
  namespace: kuberag
  labels:
    app: kuberag-api

spec:
  type: NodePort

  selector:
    app: kuberag-api

  ports:
  - name: http
    port: 80
    targetPort: 8000
    nodePort: 30080
